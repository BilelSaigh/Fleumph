{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build --prefix fleumph",
    "test": "npm run test --prefix fleumph"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}
